language: python

python:
  - "3.8"

install:
  - pip install -r requirements.txt
  - pip install --upgrade 'sentry-sdk[flask]'

script:
  - flake8 -v
  - pytest -v
  - mypy --config-file setup.cfg .
